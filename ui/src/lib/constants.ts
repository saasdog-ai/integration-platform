/** Default page size for paginated lists */
export const DEFAULT_PAGE_SIZE = 20

/** Number of recent jobs to show on the integration detail page */
export const RECENT_JOBS_LIMIT = 5

/** Auto-refresh interval for sync jobs list (ms) */
export const SYNC_JOBS_REFETCH_INTERVAL_MS = 3_000

/** Auto-refresh interval for individual job detail (ms) */
export const JOB_DETAIL_REFETCH_INTERVAL_MS = 2_000

/** Grace period before disabling auto-refresh after jobs complete (ms) */
export const REFRESH_GRACE_PERIOD_MS = 10_000
